module.exports = {
    // Jugador:
    ACCEL: 10, // Aceleracion por segundo
    MAX_SPEED: 2, // Multiplicador de velocidad maxima

    // Mobs
    MOB_ACCEL: 10,
    MOB_MAX_CHUNK: 10 // Maximo de enemigos por chunk
}